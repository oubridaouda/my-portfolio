import React from 'react';
import Navigation from "../component/Navigation";

const Home = () => {
    return (
        <div className="home">
            <Navigation/>
        </div>
    );
};

export default Home;
